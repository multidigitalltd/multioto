=== Multi Digital Agent ===
Contributors: multidigital
Requires at least: 6.0
Requires PHP: 7.4
Stable tag: 1.0.0
License: GPLv2 or later

מחבר את אתר הוורדפרס לפאנל התפעול של Multi Digital: נקודת קצה MCP מאובטחת
לאבחון ותיקון מרחוק — תמיד רק אחרי אישור צוות — ועדכון עצמי של התוסף.

== מאיפה מורידים את קובץ ה-ZIP ==

הקובץ המוכן להתקנה נמצא ב-repo תחת: wordpress-plugin/releases/multioto-agent-<גרסה>.zip
לבנייה מחדש מהמקור: הריצו wordpress-plugin/build.sh (יוצר גם dist/<גרסה>.zip וגם עותק ב-releases/).

== התקנה וחיבור ==

1. התקינו את התוסף (העלאת ה-ZIP מ-releases/ ב"תוספים → הוסף חדש → העלאת תוסף") והפעילו.
2. בפאנל של Multi Digital, במסך האתר:
   א. מלאו "כתובת MCP": https://<הדומיין-של-האתר>/wp-json/md-agent/v1/mcp
   ב. מלאו "מפתח MCP" (מפתח סודי שתבחרו).
   ג. לחצו "טוקן חיבור" וקבלו את הטוקן (מוצג פעם אחת).
3. באתר, ב"הגדרות → Multi Digital Agent", הדביקו:
   - כתובת הפאנל (למשל https://panel.multidigital.co.il)
   - אותו "מפתח MCP" מסעיף 2ב
   - "טוקן עדכון" = הטוקן מסעיף 2ג
4. שמרו. הסטטוס יעבור ל"מחובר". בפאנל אפשר ללחוץ "בדוק חיבור AI".

== אבטחה ==

* כל בקשה מהפאנל מאומתת במפתח ה-MCP (השוואת hash בזמן קבוע).
* התוסף חושף רשימה סגורה של פעולות בלבד — אין הרצת קוד/SQL/קבצים שרירותית.
* קריאת הגדרות מוגבלת לרשימת מפתחות בטוחה (ללא סודות/PII).
* שינויים (עדכון/הפעלה/כיבוי תוסף, ניקוי מטמון) מבוצעים רק לאחר אישור מנהל
  בפאנל — התוסף עצמו לא יוזם דבר.

== עדכון עצמי ==

התוסף בודק מול הפאנל אם יש גרסה חדשה ומתעדכן דרך מנגנון העדכונים הרגיל של
וורדפרס. אין צורך להתקין ידנית מחדש בכל אתר בכל גרסה.

== שרתים שמסירים את כותרת Authorization ==

חלק מהשרתים (PHP-CGI/FastCGI) מסירים את כותרת Authorization. אם "בדיקת החיבור"
נכשלת עם 401 למרות מפתח נכון, הוסיפו ל-.htaccess:

  RewriteEngine On
  RewriteCond %{HTTP:Authorization} ^(.*)
  RewriteRule .* - [E=HTTP_AUTHORIZATION:%1]

== Changelog ==

= 1.0.0 =
* גרסה ראשונה: נקודת קצה MCP מאובטחת, כלי אבחון ותיקון מוגבלים, ועדכון עצמי.
